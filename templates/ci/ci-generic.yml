name: CI Pipeline

on:
  push:
    branches: [main, 'sprint-*/**']
    paths-ignore:
      - 'docs/**'
      - '*.md'
      - '.kiro/**'
  pull_request:
    branches: [main]

jobs:
  lint-test-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # --- Choose your runtime setup ---

      # Node.js
      # - uses: actions/setup-node@v4
      #   with:
      #     node-version: '22'
      # - run: npm ci

      # Python
      # - uses: actions/setup-python@v5
      #   with:
      #     python-version: '3.12'
      # - run: pip install -r requirements.txt

      # Go
      # - uses: actions/setup-go@v5
      #   with:
      #     go-version: '1.22'
      # - run: go mod download

      # Rust
      # - uses: dtolnay/rust-toolchain@stable
      # - run: cargo fetch

      # --- Install dependencies ---
      - name: Install dependencies
        run: echo "TODO: Add install command"

      # --- Lint / Format ---
      - name: Lint
        run: echo "TODO: Add lint command"

      # --- Run tests ---
      - name: Test
        run: echo "TODO: Add test command"

      # --- Coverage check ---
      - name: Check coverage
        run: |
          echo "TODO: Add coverage extraction"
          # COVERAGE=$(extract coverage percentage)
          # THRESHOLD=70
          # if [ "$COVERAGE" -lt "$THRESHOLD" ]; then
          #   echo "Coverage $COVERAGE% below threshold $THRESHOLD%"
          #   exit 1
          # fi

      # --- Build ---
      - name: Build
        run: echo "TODO: Add build command"
