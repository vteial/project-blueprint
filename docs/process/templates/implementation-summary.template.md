[← Back to Sprint Dossier](./README.md)

# Sprint NN — Implementation Summary

> **Author:** the coding agent. Written after implementation, with the PR. Filled
> against [`spec.md`](./spec.md). Factual and terse — the audit record of *what was
> actually built*, not a re-plan.

## PR

- **PR:** #___ (`<branch>` → `main`) · **Commits:** `<sha>` … `<sha>`

## What was implemented (by spec task)

| Spec Task | File(s) | Done | Notes / deviations |
| :--- | :--- | :--: | :--- |
| NN.1 | | ✅ | |

## Deviations from the spec

_Anything that differs from the spec. If none: "None — implemented as specced."_

## Source-derived values flagged for review

_Values the spec flagged as "derive from source → verify": the value + the exact
source citation, so the reviewer can cross-check before merge._

## Open questions / follow-ups
